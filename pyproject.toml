[project]
name = "matrix-one-supplier-network"
version = "0.1.0"
description = "Matrix One 中国供应网络：供应商接入、商品主数据、报价与库存协同。"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py312"
