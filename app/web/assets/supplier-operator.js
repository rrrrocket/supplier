async function loadSupplierOperatorCooperations(){const tbody=document.querySelector("#supplier-operator-tbody");try{const rows=await Matrix.api("/api/supplier-operator/cooperations");tbody.innerHTML=rows.length?rows.map(c=>`<tr><td><strong>${Matrix.escapeHtml(c.operator_name||c.operator_id)}</strong></td><td>${Matrix.escapeHtml((c.categories||[]).join("、")||c.message||"—")}</td><td>${Matrix.statusBadge(c.status)}</td><td>${Matrix.formatDate(c.created_at,true)}</td><td><div class="row">${c.status==="PENDING"?`<button class="btn btn-primary btn-sm" data-accept="${c.id}">接受</button><button class="btn btn-danger btn-sm" data-reject="${c.id}">拒绝</button>`:""}${c.status==="ACTIVE"?`<button class="btn btn-danger btn-sm" data-terminate="${c.id}">终止</button>`:""}</div></td></tr>`).join(""):'<tr><td colspan="5" class="table-empty">暂无运营商合作申请</td></tr>';tbody.querySelectorAll("[data-accept]").forEach(b=>b.onclick=()=>actSupplierCooperation(b.dataset.accept,"accept"));tbody.querySelectorAll("[data-reject]").forEach(b=>b.onclick=()=>actSupplierCooperation(b.dataset.reject,"reject"));tbody.querySelectorAll("[data-terminate]").forEach(b=>b.onclick=()=>actSupplierCooperation(b.dataset.terminate,"terminate"));}catch(e){tbody.innerHTML=`<tr><td colspan="5" class="table-empty">${Matrix.escapeHtml(e.message)}</td></tr>`;}}
async function actSupplierCooperation(id,action){if(action==="terminate"&&!confirm("确认终止合作并使 ERP 绑定失效？"))return;const notes=action==="terminate"?undefined:prompt(action==="accept"?"确认接受，可填写备注：":"确认拒绝，可填写原因：","");if(action!=="terminate"&&notes===null)return;await Matrix.api(`/api/supplier-operator/cooperations/${id}/${action}`,{method:"POST",...(action!=="terminate"?{body:{notes}}:{})});Matrix.toast("操作成功","合作状态已更新。","success");loadSupplierOperatorCooperations();}
window.loadSupplierOperatorCooperations=loadSupplierOperatorCooperations;document.addEventListener("DOMContentLoaded",()=>document.querySelector("#refresh-supplier-operator")?.addEventListener("click",loadSupplierOperatorCooperations));
